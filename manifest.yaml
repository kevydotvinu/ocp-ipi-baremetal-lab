variables:
  stream: stable
  prod: false

releasever: 37

ref: oibl/${basearch}/coreos/${stream}
include: manifests/fedora-coreos.yaml

packages:
  - qemu-kvm
  - libvirt-daemon
  - virt-install
  - ipmitool
  - make
  - cockpit-ostree
  - cockpit-podman
  - cockpit-machines
  - cockpit-storaged
  - cockpit-networkmanager
  - openvswitch
  - toolbox

repos:
  - fedora
  - fedora-updates
